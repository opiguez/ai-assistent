# SYSTEM.md — Архитектура и системные требования Assist

## 1. Описание проекта

**Assist** — платформа для автоматизации разработки low-code систем через multi-agent AI-оркестратор с использованием MCP-протокола.

**Архитектура (два компонента):**

| Компонент | Роль | Технологии |
|---|---|---|
| **MCP Data Server** | Прослойка-адаптер между AI и low-code платформой. Предоставляет CRUD-инструменты для управления схемой. | TypeScript, Express, MCP, GraphQL (genql), Zod |
| **AI Agent (OpenCode)** | Multi-agent система из 3 агентов: **Orchestrator** (координатор) → **Plan-Execute** (архитектор, декомпозиция ТЗ) → **Build-Execute** (инженер, выполнение). | OpenCode CLI, MCP client, Ollama / внешний LLM |

**Workflow:** Пользователь описывает ТЗ → Оркестратор вызывает Plan-Execute для декомпозиции → Build-Execute построчно выполняет задачи через MCP-инструменты, создавая объектную модель на low-code платформе.

---

## 2. Модели ИИ

Специфика low-code — **метапрограммирование и code generation**: агент генерирует структуры типов данных, полей, связей, lifecycles — по сути пишет «мета-программу», транслируемую в объекты платформы. Требования к модели:
- Глубокое понимание **ООП** (наследование, полиморфизм, SOLID, baseType, childrenTypes)
- **Метапрограммирование** (code as data, рефлексия над схемой, генерация по шаблону)
- **Архитектурная рефлексия** (декомпозиция ТЗ, соблюдение контрактов и инвариантов)
- **Длинный контекст 200K+** (schema-state всего приложения + полное ТЗ + очередь задач)

### 2.1 Локальные модели (Ollama/vLLM, закрытый контур)

При локальном развертывании в квантованном виде (индустриальный стандарт **Q4_K_M** / **IQ4_NL**) модели делятся на три класса: быстрые ассистенты (до 16B), сбалансированные рабочие лошадки (27B–34B) и тяжелые экспертные системы (MoE 235B+).

| Модель | Параметры | Контекст | Реальный VRAM (Q4) | ООП / Логика | Архитектура / Скорость | Метапрог. |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Qwen3-Coder-14B** | 14B | 32K (YaRN → 128K) | ~11-12 GB | ★★★☆ | ★★★★ (Высокая) | ★★★☆ |
| **Qwen3.6-27B-MTP** | 27B | **262K native** | ~22-24 GB (с KV) | ★★★★☆ | ★★★★★ (MTP ускорение) | ★★★★☆ |
| **Qwen3-32B** | 32B | 41K (YaRN → 128K+) | ~24-26 GB | ★★★★☆ | ★★★★☆ (Гибрид Think) | ★★★★☆ |
| **DeepSeek-Coder-V2-Lite** | 16B | 128K | ~14-15 GB | ★★★☆ | ★★★☆ (Разреженная) | ★★★★ |
| **DeepSeek-R1-Distill-Qwen-32B**| 32B | 128K | ~26-28 GB | ★★★★★ | ★★★☆☆ (Медленная/Think) | ★★★★★ |
| **Qwen3-235B-A22B** | 235B (22B act.)| **256K** (в верс. 2507) | ~140-150 GB | ★★★★★ | ★★★★☆ (MoE) | ★★★★★ |

*Примечание по VRAM:* Веса модели Q4 занимают меньше памяти, но для удержания контекста свыше 32К требуется огромный объем VRAM под KV-cache (даже при использовании квантования кэша Q4_0 / Q8_0). Поэтому для моделей 27B-32B в промышленной эксплуатации строго необходима карта на **24 GB** (RTX 3090/4090/5090) или **40-80 GB** (A100/H100) или аналоги.


### 2.2 Внешние российские провайдеры

| Провайдер | Актуальные модели | Размер контекста | Работа с кодом и сложными ТЗ |
| :--- | :--- | :--- | :--- |
| **Yandex Cloud** | `YandexGPT 5 Pro`, `YandexGPT 5.1 Pro` | **128K токенов** (~240 страниц текста) | **Высокая**. Появился режим *Chain-of-Reasoning* (рассуждения). Отлично справляется со сложной логикой и техническими ТЗ. |
| **GigaChat (Сбер)** | `GigaChat 3.1 Ultra`, `GigaChat 3.1 Lightning` | **131K** (Ultra) / **256K** (Lightning) | **Высокая**. Модель Ultra построена на базе MoE (702 млрд параметров). Введены глубокие логические рассуждения, код пишет на уровне сильных зарубежных open-source моделей. |



> **Важно:** Для задач метапрограммирования и длинных архитектурных ТЗ контекст 32K недостаточен — он не вмещает schema-state + полное ТЗ + очередь задач. Рекомендуется использовать локальные модели Qwen3 при работе с реальными проектами.

---

## Рекомендации по выбору архитектуры под инфраструктурные сценарии

| Сценарий и Ограничения | Рекомендуемая модель | Обоснование и Техническая специфика |
| :--- | :--- | :--- |
| **Consumer-grade GPU<br>(12–16 GB VRAM)** | **Qwen3-Coder-14B** | Лучший кодогенератор в легковесном классе. При контексте до 32K полностью помещается в память одной доступной карты. |
| **Флагманский Single-GPU<br>(24 GB VRAM) / Макс. Скорость** | **★ Qwen3.6-27B-MTP** | **Главный хит года.** Нативно поддерживает контекст 262K. За счет архитектуры Multi-Token Prediction (MTP) выдает двукратную скорость генерации (~75-130 t/s) на локальном инференсе. |
| **Глубокая логика и аудит<br>(24 GB VRAM) / Медленный Think** | **DeepSeek-R1-Distill-Qwen-32B** | Если задача — не просто писать код, а проектировать архитектуру, искать уязвимости или разбирать запутанные ТЗ. Модель «думает» перед ответом, выстраивая скрытые логические цепочки. |
| **Enterprise Multi-GPU стойка<br>(от 160 GB VRAM до HGX 8xH100)** | **Qwen3-235B-A22B** | Флагманская MoE-модель. В обновленной архитектуре (сборка 2507+) разработчики дали нативный контекст 256K и убрали медленный гибридный think-режим, сделав её ультимативным решением для enterprise-автоматизации. |
| **Отсутствие GPU ускорителей<br>(Инференс на CPU + RAM)** | **Запрет эксплуатации** | Локальный инференс моделей крупнее 14B на CPU падает до неприемлемых 1-3 токенов в секунду. В этом сценарии контур обязан смотреть во внешние Enterprise-облака (Yandex Cloud / GigaChat Space). |

---

## Железо для локальной разработки и инференса

Расчет оборудования выполнен с учетом квантования весов (**FP8** или **Int4**), что является индустриальным стандартом для локального инференса без ощутимой потери качества ответов.

### Сценарий 1: Базовый (Тесты, простые ТЗ, суммаризация, модели до 32B параметров)
*Цель: Быстрый старт, внутренние ассистенты, базовая автоматизация.*
*   **GPU (Видеопамять):** 1 × NVIDIA A100 (80GB) или 1 × NVIDIA H100 (80GB).
*   **RAM (Оперативная память):** от 128 GB.
*   **CPU:** от 16 ядер (Intel Xeon / AMD EPYC).
*   **Дисковая подсистема:** от 1 TB NVMe SSD (скорость чтения критична для загрузки весов).

### Сценарий 2: Корпоративный стандарт (Сложные ТЗ, кодинг, аналитика, модели 70B–72B)
*Цель: Полноценная замена коммерческих облачных API для целых отделов. Модели уровня Qwen-2.5-72B требуют объединения памяти карт через NVLink.*
*   **GPU (Видеопамять):** 2 × NVIDIA A100 (80GB) или 2 × NVIDIA H100 (80GB) (общий объем VRAM 160GB).
*   **RAM (Оперативная память):** от 256 GB.
*   **CPU:** от 32 ядер.
*   **Сетевой интерфейс:** 10 GbE (для синхронизации с корпоративными базами данных / RAG).

### Сценарий 3: Максимальный (Флагманские MoE и Reasoning модели: DeepSeek-V4, DeepSeek-R1)
*Цель: Глубокие логические рассуждения (Reasoning), тяжелая разработка ПО, обслуживание всей организации.*
*   **GPU (Видеопамять):** Серверная нода (HGX архитектура) на **8 × NVIDIA H100 (80GB)** или **8 × NVIDIA A100 (80GB)**. Для моделей уровня 671B это необходимый минимум для удержания контекста 128K.
*   **RAM (Оперативная память):** от 1 TB.
*   **Системная сеть:** Коммутаторы InfiniBand или RoCE (минимальные задержки при обмене данными между GPU).